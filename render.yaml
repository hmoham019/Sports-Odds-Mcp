services:
  - type: web
    name: odds-api-mcp-server
    env: node
    buildCommand: npm install
    startCommand: node http-mcp-server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
